#!/bin/bash
#apt install fcitx-table-wbpy fcitx-googlepinyin
#sudo apt install libopencc2 fcitcx-libs fcitx-libs-qt
sudo apt install -y nodejs

sudo apt install golang

sudo apt install tlp tlp-rdw
# sudo sensors-detect

sudo apt install lm-sensors chromium
# sudo tlp start

# sudo apt install chromium

# sudo npm i gulp-cli -g
# sudo dpkg -i ***lanten.deb
sudo apt install libappindicator3-1

sudo apt install ruby-full rubygems
sudo gem sources --add https://ruby.taobao.org/ --remove https://rubygems.org/
sudo su -c "gem install sass"
